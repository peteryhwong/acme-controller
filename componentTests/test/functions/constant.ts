import * as localJson from '../../resource/local.json';

export const ENV = process.env;

export const LOCAL = localJson;

export const DEFAULT_LOCATION = 'DEFAULT_LOCATION';

export const DEFAULT_LOCATION_2 = 'DEFAULT_LOCATION_2';
